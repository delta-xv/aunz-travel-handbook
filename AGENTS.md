# 项目规则

- 所有设备共用手机界面，最大宽度 430px，宽屏居中展示；导航、行程详情和出团通知保持一致，不单独设计桌面布局。
- 保留清楚的触摸操作和简洁文案。
- 不添加夜间模式、重复副标题或冗长使用说明。
- 修改时优先做代码检查和针对性功能检查，避免反复截图；用户会自行体验。
- 原始出团通知 PDF 只保存在仓库外，不提交原稿或派生页面图片。网页按目录和章节呈现 HTML 正文，合并排版换行，仅删除用户指定内容。图示转为文字。
- 发布内容不包含领队姓名、联系方式或带有这些信息的接机牌内容。

## 发布

- GitHub 仓库：`delta-xv/aunz-travel-handbook`。
- GitHub Pages 从 `main` 分支根目录发布。
- 每次完成修改后，检查相关功能，运行 `node scripts/version-assets.mjs` 更新资源版本，提交并推送到 `main`，然后确认对应提交的 Pages 构建结果。
- 提交标题使用 `<type>(<scope>): <中文简述>`。
- 只提交本次任务的修改，不覆盖用户的其他改动，不强制推送。
- 发布成功后提供线上链接；构建失败或无法推送时如实说明。
- 线上地址：https://delta-xv.github.io/aunz-travel-handbook/
